services:
  - type: web
    name: meet-ai-scribe-playwright-bot
    runtime: docker
    dockerfilePath: ./Dockerfile.playwright-bot
    plan: free
    autoDeploy: true
    envVars:
      - key: PORT
        value: 3001
